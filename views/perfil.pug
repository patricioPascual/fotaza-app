extends layout 

block head
  title Fotaza - Perfil
  link(rel="stylesheet", href="/css/Style.css")
  link(rel="stylesheet", href="/css/perfil.css")

block main_content
  div.container-principal
    include partials/sidebar
    
    main#muro-principal
      div.perfil-header
        
        form(id="form-perfil" action="/perfil/subir-foto" method="POST")
          div.contenedor-foto-perfil
            if usuario && usuario.fotoPerfil
             img.foto-usuario(src=`data:image/jpeg;base64,${usuario.fotoPerfil.toString('base64')}`, alt="Foto de perfil")
            else 
             img.foto-usuario(src="/img/user-default.jpg", alt="Foto de perfil")
            
            label.btn-camara(for="input-foto")
              span 📷
              
              input(type="file" id="input-foto"  accept="image/*" style="display:none;")
        
        div.info-usuario
          h2 Tu Perfil
          if usuario
            h3 Nombre de usuario: #{usuario.nombre}
            h4 Email: #{usuario.email}
          else
            p Cargando datos de usuario...

      div.resultados
        h2 Tus Publicaciones
        if publicaciones && publicaciones.length > 0
          each pub in publicaciones
            
            include partials/publicaciones 
        else
          p Aún no tienes publicaciones.

  
  script(src="/js/perfil.js")